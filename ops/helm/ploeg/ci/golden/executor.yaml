---
# Source: ploeg/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ploeg
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
automountServiceAccountToken: false
---
# Source: ploeg/templates/serviceaccount.yaml
# The workers' own identity, separate from ploegd's.
#
# Worker pods used to name no ServiceAccount at all, so the API server gave
# them `default` — which is what `require-non-default-serviceaccount` fires on,
# and it means every workload in the namespace shares one identity in audit
# logs. They already run with automountServiceAccountToken: false and take zero
# Kubernetes API authority, so this is an identity and audit fix rather than a
# privilege one: the LLM-driven process still gets no token.
#
# Deliberately NOT ploegd's account. ploegd holds the LiteLLM master key and
# (when configured) the forge admin credential; giving the agent pods the same
# name would make a future RoleBinding on it apply to both.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ploeg-worker
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
automountServiceAccountToken: false

---
# Source: ploeg/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: ploeg
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
  ports:
    - name: http
      port: 8080
      targetPort: http

---
# Source: ploeg/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ploeg
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ploeg
      app.kubernetes.io/instance: ploeg
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ploeg
        app.kubernetes.io/instance: ploeg
    spec:
      serviceAccountName: ploeg
      automountServiceAccountToken: false
      nodeSelector:
        node.webgrip.io/pool: worker
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: ploegd
          image: harbor.webgrip.dev/webgrip/ploegd:CHART-VERSION
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: PLOEG_LISTEN
              value: ":8080"
            - name: PLOEG_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ploeg-db-app
                  key: uri
            - name: PLOEG_VIKUNJA_SECRET
              valueFrom:
                secretKeyRef:
                  name: ploeg-webhook-secret
                  key: PLOEG_VIKUNJA_SECRET
            - name: PLOEG_TEAM_PLANS
              value: "{\"gold\":{\"maxFixRounds\":0,\"pool\":\"6\",\"rounds\":[{\"roles\":[{\"cap\":\"0.50\",\"harness\":{\"dind\":false},\"name\":\"analyst\",\"writes\":false},{\"cap\":\"0.50\",\"harness\":{\"dind\":false},\"name\":\"tests\",\"writes\":false}]},{\"roles\":[{\"cap\":\"3.00\",\"model\":\"litellm_proxy/claude-sonnet-5\",\"name\":\"builder\",\"writes\":true}]},{\"roles\":[{\"cap\":\"0.75\",\"harness\":{\"dind\":false},\"model\":\"litellm_proxy/claude-sonnet-5\",\"name\":\"reviewer\",\"writes\":false}]},{\"roles\":[{\"cap\":\"3.00\",\"model\":\"litellm_proxy/claude-sonnet-5\",\"name\":\"builder\",\"writes\":true}]}]}}"
            - name: PLOEG_FORGEJO_URL
              value: "http://forgejo-http.forgejo.svc.cluster.local:3000"
            - name: PLOEG_FORGEJO_TOKEN
              valueFrom:
                secretKeyRef:
                  name: agent-builder-token
                  key: FORGEJO_TOKEN
            - name: PLOEG_FORGEJO_BOT
              value: "agent-builder"
            - name: PLOEG_TARGET_FORGE
              value: "forgejo"
            - name: PLOEG_DEFAULT_TEAM
              value: "default"
            - name: PLOEG_LEASE_TTL
              value: "15m"
            - name: PLOEG_SWEEP_INTERVAL
              value: "15s"
            - name: PLOEG_TEAM_MAP
              value: ""
            - name: LITELLM_ADMIN_URL
              value: "http://litellm.ai.svc.cluster.local:4000"
            - name: LITELLM_MASTER_KEY
              valueFrom:
                secretKeyRef:
                  name: agent-litellm-master
                  key: LITELLM_MASTER_KEY
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 20
          resources:
            limits:
              cpu: 100m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 256Mi

---
# Source: ploeg/templates/scaledjob.yaml
# One ScaledJob per (team, Role) — the flagship executor. A team without a
# plan has exactly one Role-less workload, byte-identical to the pre-Shift
# shape; a team with a plan gets one per Role, which is what makes "different
# agents, different harnesses, different images" literally true: pod shape is
# fixed at render time. The shape lives in the shared ploeg.workerPodTemplate
# helper (also used by the CronJob executor): privileged DinD sidecar when the
# harness needs Docker, a shared disk-backed workspace mounted at the SAME
# path in both containers, and zero Kubernetes API authority for the
# LLM-driven process.
apiVersion: keda.sh/v1alpha1
kind: ScaledJob
metadata:
  name: ploeg-worker-bronze
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
    # KEDA copies these labels onto every Job it creates, which is the only way
    # the hazard reaches the Job. Kyverno autogens a Job rule for
    # pod-security-baseline-enforce and a Job selector matches the JOB's own
    # labels — stamping this on the pod template alone left the PolicyException
    # unable to admit the Job, and DinD teams were rejected at admission.
    ploeg.webgrip.dev/privileged-dind: "true"
spec:
  pollingInterval: 30
  minReplicaCount: 0
  maxReplicaCount: 1
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  rollout:
    strategy: gradual # spec updates never kill in-flight runs (backlog #50)
  scalingStrategy:
    strategy: accurate # never eager: open over-scaling bug kedacore/keda#6416 (backlog #47)
  jobTargetRef:
    parallelism: 1
    completions: 1
    backoffLimit: 0 # ploeg owns retries; every pod is one auditable run (backlog #51)
    activeDeadlineSeconds: 7200
    template:
      
      
      metadata:
        labels:
          app.kubernetes.io/name: ploeg-worker
          ploeg.webgrip.dev/team: bronze
          # Names the HAZARD, not the workload: this pod carries a privileged
          # docker:dind sidecar, and the ploeg-worker PolicyException is keyed on
          # exactly this label. A pod without dind never carries it and is held to
          # the full baseline — so adding a Role costs no security-repo change, and
          # readers are not waived for a privilege they do not take.
          ploeg.webgrip.dev/privileged-dind: "true"
      spec:
        restartPolicy: Never
        # The shutdown budget for ploeg-worker's SIGTERM path: abort the harness,
        # revoke the per-run credential, settle its spend, report the outcome. The
        # 30s default SIGKILLed the pod mid-report, which is the silence that made a
        # killed run indistinguishable from a hung one and cost the Round an attempt.
        terminationGracePeriodSeconds: 90
        # An identity of the workers' own. Naming none left them on `default`, which
        # both trips require-non-default-serviceaccount and makes every workload in
        # the namespace indistinguishable in an audit log. The token stays unmounted
        # either way — a worker needs no Kubernetes API authority at all.
        serviceAccountName: ploeg-worker
        automountServiceAccountToken: false
        nodeSelector:
          node.webgrip.io/pool: worker
        securityContext:
          fsGroup: 1000
        initContainers:
          # Extracts ploeg-worker from the distroless ploegd image via
          # self-copy (no shell in distroless).
          - name: worker-bin
            image: harbor.webgrip.dev/webgrip/ploegd:CHART-VERSION
            command: ["/usr/local/bin/ploeg-worker", "install", "/mnt/bin/ploeg-worker"]
            securityContext:
              runAsNonRoot: true
              runAsUser: 65532
              allowPrivilegeEscalation: false
              capabilities:
                drop: [ALL]
              seccompProfile:
                type: RuntimeDefault
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
            resources:
              limits:
                cpu: 50m
                memory: 64Mi
              requests:
                cpu: 50m
                memory: 64Mi
          # DinD sidecar: the harness sandbox and the repo quality gates need a
          # Docker daemon. Requires the ploeg-worker PolicyException. Teams whose
          # harness needs no Docker set harness.dind: false.
          - name: dind
            image: harbor.webgrip.dev/dockerhub/library/docker:29.6.2-dind
            restartPolicy: Always
            securityContext:
              privileged: true
            env:
              - name: DOCKER_TLS_CERTDIR
                value: /certs
            startupProbe:
              tcpSocket:
                port: 2376
              periodSeconds: 2
              failureThreshold: 60
              timeoutSeconds: 5
            volumeMounts:
              - name: docker-certs
                mountPath: /certs
              - name: docker-storage
                mountPath: /var/lib/docker
              - name: ci-shared
                mountPath: /mnt/ci-shared
            resources:
              limits:
                cpu: "1"
                memory: 1536Mi
              requests:
                cpu: "1"
                memory: 1536Mi
        containers:
          - name: worker
            image: harbor.webgrip.dev/webgrip/agent-runner:1.0.2@sha256:086b4bc0d3fda0e3c179f9501bb46a612f5f33e60d96405a5d94471a26279596
            command: ["/mnt/bin/ploeg-worker"]
            env:
              - name: PLOEG_API_URL
                value: "http://ploeg:8080"
              - name: PLOEG_TEAM
                value: "bronze"
              - name: PLOEG_HARNESS
                value: "openhands"
              - name: LLM_MODEL
                value: "litellm_proxy/deepseek-chat"
              - name: LITELLM_KEY_BUDGET
                value: "2"
              - name: LITELLM_KEY_DURATION
                value: "4h"
              - name: LLM_BASE_URL
                value: "http://litellm.ai.svc.cluster.local:4000/v1"
              - name: LITELLM_ADMIN_URL
                value: "http://litellm.ai.svc.cluster.local:4000"
              - name: LITELLM_MASTER_KEY
                valueFrom:
                  secretKeyRef:
                    name: agent-litellm-master
                    key: LITELLM_MASTER_KEY
              - name: PLOEG_TARGET_FORGE
                value: "forgejo"
              - name: FORGE_URL
                value: "http://forgejo-http.forgejo.svc.cluster.local:3000"
              - name: AGENT_BUILDER_TOKEN
                valueFrom:
                  secretKeyRef:
                    name: agent-builder-token
                    key: FORGEJO_TOKEN
              # FALLBACK target only. The repository belongs to the work item,
              # resolved at ingest from its tracker scope and delivered on the claim
              # (R11, ADR-0001); these render only while a team still pins a repo,
              # and a team that declares none renders no repo env at all.
              - name: REPO_OWNER
                value: "webgrip"
              - name: REPO_NAME
                value: "example"
              # Base branch for clone/branch/PR (VIK-589). Unset = the repo's
              # default branch on clone and the worker's historical "main".
              - name: PLOEG_BASE_BRANCH
                value: "development"
              # Downward API: node+pod identity survives pod/job cleanup for
              # run forensics (VIK-597). Logged at worker start and embedded in
              # every checkpoint.
              - name: NODE_NAME
                valueFrom:
                  fieldRef:
                    fieldPath: spec.nodeName
              - name: POD_UID
                valueFrom:
                  fieldRef:
                    fieldPath: metadata.uid
              - name: WORK_DIR
                value: /mnt/ci-shared
              - name: DOCKER_HOST
                value: tcp://localhost:2376
              - name: DOCKER_CERT_PATH
                value: /certs/client
              - name: DOCKER_TLS_VERIFY
                value: "1"
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
                readOnly: true
              - name: docker-certs
                mountPath: /certs
                readOnly: true
              - name: ci-shared
                mountPath: /mnt/ci-shared
            resources:
              limits:
                cpu: "1"
                memory: 1Gi
              requests:
                cpu: "1"
                memory: 1Gi
        volumes:
          - name: worker-bin
            emptyDir: {}
          - name: docker-certs
            emptyDir: {}
          - name: docker-storage
            emptyDir: {}
          # Disk-backed (not tmpfs): repo checkout + gate builds are large.
          # Same absolute path in worker AND dind so `docker run -v`
          # bind-mounts resolve on the daemon's filesystem.
          - name: ci-shared
            emptyDir:
              sizeLimit: 8Gi
  triggers:
    - type: postgresql
      metadata:
        # Resolved by the KEDA operator from ITS namespace, not this one —
        # a bare in-namespace service name never resolves there.
        host: "ploeg-db-rw.default.svc.cluster.local"
        port: "5432"
        userName: "ploeg_scaler"
        dbName: "app"
        sslmode: "require"
        # The one poll-query shape: served index-only by work_items_claimable
        # (backlog #24/#47). Must stay in sync with store.Claim/QueueDepth.
        query: "SELECT COUNT(*) FROM work_items WHERE team = 'bronze' AND state = 'queued' AND (next_eligible_at IS NULL OR next_eligible_at <= now())"
        targetQueryValue: "1"
        activationTargetQueryValue: "0"
      authenticationRef:
        name: ploeg-scaler
---
# Source: ploeg/templates/scaledjob.yaml
# One ScaledJob per (team, Role) — the flagship executor. A team without a
# plan has exactly one Role-less workload, byte-identical to the pre-Shift
# shape; a team with a plan gets one per Role, which is what makes "different
# agents, different harnesses, different images" literally true: pod shape is
# fixed at render time. The shape lives in the shared ploeg.workerPodTemplate
# helper (also used by the CronJob executor): privileged DinD sidecar when the
# harness needs Docker, a shared disk-backed workspace mounted at the SAME
# path in both containers, and zero Kubernetes API authority for the
# LLM-driven process.
apiVersion: keda.sh/v1alpha1
kind: ScaledJob
metadata:
  name: ploeg-worker-copper
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
spec:
  pollingInterval: 30
  minReplicaCount: 0
  maxReplicaCount: 1
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  rollout:
    strategy: gradual # spec updates never kill in-flight runs (backlog #50)
  scalingStrategy:
    strategy: accurate # never eager: open over-scaling bug kedacore/keda#6416 (backlog #47)
  jobTargetRef:
    parallelism: 1
    completions: 1
    backoffLimit: 0 # ploeg owns retries; every pod is one auditable run (backlog #51)
    activeDeadlineSeconds: 7200
    template:
      
      
      metadata:
        labels:
          app.kubernetes.io/name: ploeg-worker
          ploeg.webgrip.dev/team: copper
      spec:
        restartPolicy: Never
        # The shutdown budget for ploeg-worker's SIGTERM path: abort the harness,
        # revoke the per-run credential, settle its spend, report the outcome. The
        # 30s default SIGKILLed the pod mid-report, which is the silence that made a
        # killed run indistinguishable from a hung one and cost the Round an attempt.
        terminationGracePeriodSeconds: 90
        # An identity of the workers' own. Naming none left them on `default`, which
        # both trips require-non-default-serviceaccount and makes every workload in
        # the namespace indistinguishable in an audit log. The token stays unmounted
        # either way — a worker needs no Kubernetes API authority at all.
        serviceAccountName: ploeg-worker
        automountServiceAccountToken: false
        nodeSelector:
          node.webgrip.io/pool: worker
        securityContext:
          fsGroup: 1000
        initContainers:
          # Extracts ploeg-worker from the distroless ploegd image via
          # self-copy (no shell in distroless).
          - name: worker-bin
            image: harbor.webgrip.dev/webgrip/ploegd:CHART-VERSION
            command: ["/usr/local/bin/ploeg-worker", "install", "/mnt/bin/ploeg-worker"]
            securityContext:
              runAsNonRoot: true
              runAsUser: 65532
              allowPrivilegeEscalation: false
              capabilities:
                drop: [ALL]
              seccompProfile:
                type: RuntimeDefault
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
            resources:
              limits:
                cpu: 50m
                memory: 64Mi
              requests:
                cpu: 50m
                memory: 64Mi
        containers:
          - name: worker
            image: harbor.webgrip.dev/webgrip/custom-agent:0.1.0
            command: ["/mnt/bin/ploeg-worker"]
            env:
              - name: PLOEG_API_URL
                value: "http://ploeg:8080"
              - name: PLOEG_TEAM
                value: "copper"
              - name: PLOEG_HARNESS
                value: "exec"
              - name: PLOEG_HARNESS_ARGS
                value: "[\"/usr/local/bin/agent\",\"--spec\",\"{taskspec}\",\"--task\",\"{taskfile}\"]"
              - name: LLM_MODEL
                value: "litellm_proxy/claude-sonnet-5"
              - name: LITELLM_KEY_BUDGET
                value: "3"
              - name: LITELLM_KEY_DURATION
                value: "4h"
              - name: LLM_BASE_URL
                value: "http://litellm.ai.svc.cluster.local:4000/v1"
              - name: LITELLM_ADMIN_URL
                value: "http://litellm.ai.svc.cluster.local:4000"
              - name: LITELLM_MASTER_KEY
                valueFrom:
                  secretKeyRef:
                    name: agent-litellm-master
                    key: LITELLM_MASTER_KEY
              - name: PLOEG_TARGET_FORGE
                value: "forgejo"
              - name: FORGE_URL
                value: "http://forgejo-http.forgejo.svc.cluster.local:3000"
              - name: AGENT_BUILDER_TOKEN
                valueFrom:
                  secretKeyRef:
                    name: agent-builder-token
                    key: FORGEJO_TOKEN
              # FALLBACK target only. The repository belongs to the work item,
              # resolved at ingest from its tracker scope and delivered on the claim
              # (R11, ADR-0001); these render only while a team still pins a repo,
              # and a team that declares none renders no repo env at all.
              - name: REPO_OWNER
                value: "webgrip"
              - name: REPO_NAME
                value: "example"
              # Base branch for clone/branch/PR (VIK-589). Unset = the repo's
              # default branch on clone and the worker's historical "main".
              - name: PLOEG_BASE_BRANCH
                value: "development"
              # Downward API: node+pod identity survives pod/job cleanup for
              # run forensics (VIK-597). Logged at worker start and embedded in
              # every checkpoint.
              - name: NODE_NAME
                valueFrom:
                  fieldRef:
                    fieldPath: spec.nodeName
              - name: POD_UID
                valueFrom:
                  fieldRef:
                    fieldPath: metadata.uid
              - name: WORK_DIR
                value: /mnt/ci-shared
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
                readOnly: true
              - name: ci-shared
                mountPath: /mnt/ci-shared
            resources:
              limits:
                cpu: "1"
                memory: 1Gi
              requests:
                cpu: "1"
                memory: 1Gi
        volumes:
          - name: worker-bin
            emptyDir: {}
          # Disk-backed (not tmpfs): repo checkout + gate builds are large.
          # Same absolute path in worker AND dind so `docker run -v`
          # bind-mounts resolve on the daemon's filesystem.
          - name: ci-shared
            emptyDir:
              sizeLimit: 8Gi
  triggers:
    - type: postgresql
      metadata:
        # Resolved by the KEDA operator from ITS namespace, not this one —
        # a bare in-namespace service name never resolves there.
        host: "ploeg-db-rw.default.svc.cluster.local"
        port: "5432"
        userName: "ploeg_scaler"
        dbName: "app"
        sslmode: "require"
        # The one poll-query shape: served index-only by work_items_claimable
        # (backlog #24/#47). Must stay in sync with store.Claim/QueueDepth.
        query: "SELECT COUNT(*) FROM work_items WHERE team = 'copper' AND state = 'queued' AND (next_eligible_at IS NULL OR next_eligible_at <= now())"
        targetQueryValue: "1"
        activationTargetQueryValue: "0"
      authenticationRef:
        name: ploeg-scaler
---
# Source: ploeg/templates/scaledjob.yaml
# One ScaledJob per (team, Role) — the flagship executor. A team without a
# plan has exactly one Role-less workload, byte-identical to the pre-Shift
# shape; a team with a plan gets one per Role, which is what makes "different
# agents, different harnesses, different images" literally true: pod shape is
# fixed at render time. The shape lives in the shared ploeg.workerPodTemplate
# helper (also used by the CronJob executor): privileged DinD sidecar when the
# harness needs Docker, a shared disk-backed workspace mounted at the SAME
# path in both containers, and zero Kubernetes API authority for the
# LLM-driven process.
apiVersion: keda.sh/v1alpha1
kind: ScaledJob
metadata:
  name: ploeg-worker-tin
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
    # KEDA copies these labels onto every Job it creates, which is the only way
    # the hazard reaches the Job. Kyverno autogens a Job rule for
    # pod-security-baseline-enforce and a Job selector matches the JOB's own
    # labels — stamping this on the pod template alone left the PolicyException
    # unable to admit the Job, and DinD teams were rejected at admission.
    ploeg.webgrip.dev/privileged-dind: "true"
spec:
  pollingInterval: 30
  minReplicaCount: 0
  maxReplicaCount: 1
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  rollout:
    strategy: gradual # spec updates never kill in-flight runs (backlog #50)
  scalingStrategy:
    strategy: accurate # never eager: open over-scaling bug kedacore/keda#6416 (backlog #47)
  jobTargetRef:
    parallelism: 1
    completions: 1
    backoffLimit: 0 # ploeg owns retries; every pod is one auditable run (backlog #51)
    activeDeadlineSeconds: 7200
    template:
      
      
      metadata:
        labels:
          app.kubernetes.io/name: ploeg-worker
          ploeg.webgrip.dev/team: tin
          # Names the HAZARD, not the workload: this pod carries a privileged
          # docker:dind sidecar, and the ploeg-worker PolicyException is keyed on
          # exactly this label. A pod without dind never carries it and is held to
          # the full baseline — so adding a Role costs no security-repo change, and
          # readers are not waived for a privilege they do not take.
          ploeg.webgrip.dev/privileged-dind: "true"
      spec:
        restartPolicy: Never
        # The shutdown budget for ploeg-worker's SIGTERM path: abort the harness,
        # revoke the per-run credential, settle its spend, report the outcome. The
        # 30s default SIGKILLed the pod mid-report, which is the silence that made a
        # killed run indistinguishable from a hung one and cost the Round an attempt.
        terminationGracePeriodSeconds: 90
        # An identity of the workers' own. Naming none left them on `default`, which
        # both trips require-non-default-serviceaccount and makes every workload in
        # the namespace indistinguishable in an audit log. The token stays unmounted
        # either way — a worker needs no Kubernetes API authority at all.
        serviceAccountName: ploeg-worker
        automountServiceAccountToken: false
        nodeSelector:
          node.webgrip.io/pool: worker
        securityContext:
          fsGroup: 1000
        initContainers:
          # Extracts ploeg-worker from the distroless ploegd image via
          # self-copy (no shell in distroless).
          - name: worker-bin
            image: harbor.webgrip.dev/webgrip/ploegd:CHART-VERSION
            command: ["/usr/local/bin/ploeg-worker", "install", "/mnt/bin/ploeg-worker"]
            securityContext:
              runAsNonRoot: true
              runAsUser: 65532
              allowPrivilegeEscalation: false
              capabilities:
                drop: [ALL]
              seccompProfile:
                type: RuntimeDefault
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
            resources:
              limits:
                cpu: 50m
                memory: 64Mi
              requests:
                cpu: 50m
                memory: 64Mi
          # DinD sidecar: the harness sandbox and the repo quality gates need a
          # Docker daemon. Requires the ploeg-worker PolicyException. Teams whose
          # harness needs no Docker set harness.dind: false.
          - name: dind
            image: harbor.webgrip.dev/dockerhub/library/docker:29.6.2-dind
            restartPolicy: Always
            securityContext:
              privileged: true
            env:
              - name: DOCKER_TLS_CERTDIR
                value: /certs
            startupProbe:
              tcpSocket:
                port: 2376
              periodSeconds: 2
              failureThreshold: 60
              timeoutSeconds: 5
            volumeMounts:
              - name: docker-certs
                mountPath: /certs
              - name: docker-storage
                mountPath: /var/lib/docker
              - name: ci-shared
                mountPath: /mnt/ci-shared
            resources:
              limits:
                cpu: "1"
                memory: 1536Mi
              requests:
                cpu: "1"
                memory: 1536Mi
        containers:
          - name: worker
            image: harbor.webgrip.dev/webgrip/agent-runner:1.0.2@sha256:086b4bc0d3fda0e3c179f9501bb46a612f5f33e60d96405a5d94471a26279596
            command: ["/mnt/bin/ploeg-worker"]
            env:
              - name: PLOEG_API_URL
                value: "http://ploeg:8080"
              - name: PLOEG_TEAM
                value: "tin"
              - name: PLOEG_HARNESS
                value: "openhands"
              - name: LLM_MODEL
                value: "litellm_proxy/deepseek-chat"
              - name: LITELLM_KEY_BUDGET
                value: "1"
              - name: LITELLM_KEY_DURATION
                value: "4h"
              - name: LLM_BASE_URL
                value: "http://litellm.ai.svc.cluster.local:4000/v1"
              - name: LITELLM_ADMIN_URL
                value: "http://litellm.ai.svc.cluster.local:4000"
              - name: LITELLM_MASTER_KEY
                valueFrom:
                  secretKeyRef:
                    name: agent-litellm-master
                    key: LITELLM_MASTER_KEY
              - name: PLOEG_TARGET_FORGE
                value: "forgejo"
              - name: FORGE_URL
                value: "http://forgejo-http.forgejo.svc.cluster.local:3000"
              - name: AGENT_BUILDER_TOKEN
                valueFrom:
                  secretKeyRef:
                    name: agent-builder-token
                    key: FORGEJO_TOKEN
              # FALLBACK target only. The repository belongs to the work item,
              # resolved at ingest from its tracker scope and delivered on the claim
              # (R11, ADR-0001); these render only while a team still pins a repo,
              # and a team that declares none renders no repo env at all.
              # Downward API: node+pod identity survives pod/job cleanup for
              # run forensics (VIK-597). Logged at worker start and embedded in
              # every checkpoint.
              - name: NODE_NAME
                valueFrom:
                  fieldRef:
                    fieldPath: spec.nodeName
              - name: POD_UID
                valueFrom:
                  fieldRef:
                    fieldPath: metadata.uid
              - name: WORK_DIR
                value: /mnt/ci-shared
              - name: DOCKER_HOST
                value: tcp://localhost:2376
              - name: DOCKER_CERT_PATH
                value: /certs/client
              - name: DOCKER_TLS_VERIFY
                value: "1"
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
                readOnly: true
              - name: docker-certs
                mountPath: /certs
                readOnly: true
              - name: ci-shared
                mountPath: /mnt/ci-shared
            resources:
              limits:
                cpu: "1"
                memory: 1Gi
              requests:
                cpu: "1"
                memory: 1Gi
        volumes:
          - name: worker-bin
            emptyDir: {}
          - name: docker-certs
            emptyDir: {}
          - name: docker-storage
            emptyDir: {}
          # Disk-backed (not tmpfs): repo checkout + gate builds are large.
          # Same absolute path in worker AND dind so `docker run -v`
          # bind-mounts resolve on the daemon's filesystem.
          - name: ci-shared
            emptyDir:
              sizeLimit: 8Gi
  triggers:
    - type: postgresql
      metadata:
        # Resolved by the KEDA operator from ITS namespace, not this one —
        # a bare in-namespace service name never resolves there.
        host: "ploeg-db-rw.default.svc.cluster.local"
        port: "5432"
        userName: "ploeg_scaler"
        dbName: "app"
        sslmode: "require"
        # The one poll-query shape: served index-only by work_items_claimable
        # (backlog #24/#47). Must stay in sync with store.Claim/QueueDepth.
        query: "SELECT COUNT(*) FROM work_items WHERE team = 'tin' AND state = 'queued' AND (next_eligible_at IS NULL OR next_eligible_at <= now())"
        targetQueryValue: "1"
        activationTargetQueryValue: "0"
      authenticationRef:
        name: ploeg-scaler
---
# Source: ploeg/templates/scaledjob.yaml
# One ScaledJob per (team, Role) — the flagship executor. A team without a
# plan has exactly one Role-less workload, byte-identical to the pre-Shift
# shape; a team with a plan gets one per Role, which is what makes "different
# agents, different harnesses, different images" literally true: pod shape is
# fixed at render time. The shape lives in the shared ploeg.workerPodTemplate
# helper (also used by the CronJob executor): privileged DinD sidecar when the
# harness needs Docker, a shared disk-backed workspace mounted at the SAME
# path in both containers, and zero Kubernetes API authority for the
# LLM-driven process.
apiVersion: keda.sh/v1alpha1
kind: ScaledJob
metadata:
  name: ploeg-worker-zinc
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
    # KEDA copies these labels onto every Job it creates, which is the only way
    # the hazard reaches the Job. Kyverno autogens a Job rule for
    # pod-security-baseline-enforce and a Job selector matches the JOB's own
    # labels — stamping this on the pod template alone left the PolicyException
    # unable to admit the Job, and DinD teams were rejected at admission.
    ploeg.webgrip.dev/privileged-dind: "true"
spec:
  pollingInterval: 30
  minReplicaCount: 0
  maxReplicaCount: 1
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  rollout:
    strategy: gradual # spec updates never kill in-flight runs (backlog #50)
  scalingStrategy:
    strategy: accurate # never eager: open over-scaling bug kedacore/keda#6416 (backlog #47)
  jobTargetRef:
    parallelism: 1
    completions: 1
    backoffLimit: 0 # ploeg owns retries; every pod is one auditable run (backlog #51)
    activeDeadlineSeconds: 7200
    template:
      
      
      metadata:
        labels:
          app.kubernetes.io/name: ploeg-worker
          ploeg.webgrip.dev/team: zinc
          # Names the HAZARD, not the workload: this pod carries a privileged
          # docker:dind sidecar, and the ploeg-worker PolicyException is keyed on
          # exactly this label. A pod without dind never carries it and is held to
          # the full baseline — so adding a Role costs no security-repo change, and
          # readers are not waived for a privilege they do not take.
          ploeg.webgrip.dev/privileged-dind: "true"
      spec:
        restartPolicy: Never
        # The shutdown budget for ploeg-worker's SIGTERM path: abort the harness,
        # revoke the per-run credential, settle its spend, report the outcome. The
        # 30s default SIGKILLed the pod mid-report, which is the silence that made a
        # killed run indistinguishable from a hung one and cost the Round an attempt.
        terminationGracePeriodSeconds: 90
        # An identity of the workers' own. Naming none left them on `default`, which
        # both trips require-non-default-serviceaccount and makes every workload in
        # the namespace indistinguishable in an audit log. The token stays unmounted
        # either way — a worker needs no Kubernetes API authority at all.
        serviceAccountName: ploeg-worker
        automountServiceAccountToken: false
        nodeSelector:
          node.webgrip.io/pool: worker
        securityContext:
          fsGroup: 1000
        initContainers:
          # Extracts ploeg-worker from the distroless ploegd image via
          # self-copy (no shell in distroless).
          - name: worker-bin
            image: harbor.webgrip.dev/webgrip/ploegd:CHART-VERSION
            command: ["/usr/local/bin/ploeg-worker", "install", "/mnt/bin/ploeg-worker"]
            securityContext:
              runAsNonRoot: true
              runAsUser: 65532
              allowPrivilegeEscalation: false
              capabilities:
                drop: [ALL]
              seccompProfile:
                type: RuntimeDefault
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
            resources:
              limits:
                cpu: 50m
                memory: 64Mi
              requests:
                cpu: 50m
                memory: 64Mi
          # DinD sidecar: the harness sandbox and the repo quality gates need a
          # Docker daemon. Requires the ploeg-worker PolicyException. Teams whose
          # harness needs no Docker set harness.dind: false.
          - name: dind
            image: harbor.webgrip.dev/dockerhub/library/docker:29.6.2-dind
            restartPolicy: Always
            securityContext:
              privileged: true
            env:
              - name: DOCKER_TLS_CERTDIR
                value: /certs
            startupProbe:
              tcpSocket:
                port: 2376
              periodSeconds: 2
              failureThreshold: 60
              timeoutSeconds: 5
            volumeMounts:
              - name: docker-certs
                mountPath: /certs
              - name: docker-storage
                mountPath: /var/lib/docker
              - name: ci-shared
                mountPath: /mnt/ci-shared
            resources:
              limits:
                cpu: "1"
                memory: 1536Mi
              requests:
                cpu: "1"
                memory: 1536Mi
        containers:
          - name: worker
            image: harbor.webgrip.dev/webgrip/agent-runner:1.0.2@sha256:086b4bc0d3fda0e3c179f9501bb46a612f5f33e60d96405a5d94471a26279596
            command: ["/mnt/bin/ploeg-worker"]
            env:
              - name: PLOEG_API_URL
                value: "http://ploeg:8080"
              - name: PLOEG_TEAM
                value: "zinc"
              - name: PLOEG_HARNESS
                value: "openhands"
              - name: LLM_MODEL
                value: "litellm_proxy/deepseek-chat"
              - name: LITELLM_KEY_BUDGET
                value: "1"
              - name: LITELLM_KEY_DURATION
                value: "4h"
              - name: LLM_BASE_URL
                value: "http://litellm.ai.svc.cluster.local:4000/v1"
              - name: LITELLM_ADMIN_URL
                value: "http://litellm.ai.svc.cluster.local:4000"
              - name: LITELLM_MASTER_KEY
                valueFrom:
                  secretKeyRef:
                    name: agent-litellm-master
                    key: LITELLM_MASTER_KEY
              - name: PLOEG_TARGET_FORGE
                value: "forgejo"
              - name: FORGE_URL
                value: "http://forgejo-http.forgejo.svc.cluster.local:3000"
              - name: AGENT_BUILDER_TOKEN
                valueFrom:
                  secretKeyRef:
                    name: agent-builder-token
                    key: FORGEJO_TOKEN
              # FALLBACK target only. The repository belongs to the work item,
              # resolved at ingest from its tracker scope and delivered on the claim
              # (R11, ADR-0001); these render only while a team still pins a repo,
              # and a team that declares none renders no repo env at all.
              - name: REPO_OWNER
                value: "webgrip"
              - name: REPO_NAME
                value: "example"
              # env = ignore the claim's target and use this team's pinned repo.
              # The per-team lever for rolling the decoupling forward or back.
              - name: PLOEG_TARGET_SOURCE
                value: "env"
              # Downward API: node+pod identity survives pod/job cleanup for
              # run forensics (VIK-597). Logged at worker start and embedded in
              # every checkpoint.
              - name: NODE_NAME
                valueFrom:
                  fieldRef:
                    fieldPath: spec.nodeName
              - name: POD_UID
                valueFrom:
                  fieldRef:
                    fieldPath: metadata.uid
              - name: WORK_DIR
                value: /mnt/ci-shared
              - name: DOCKER_HOST
                value: tcp://localhost:2376
              - name: DOCKER_CERT_PATH
                value: /certs/client
              - name: DOCKER_TLS_VERIFY
                value: "1"
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
                readOnly: true
              - name: docker-certs
                mountPath: /certs
                readOnly: true
              - name: ci-shared
                mountPath: /mnt/ci-shared
            resources:
              limits:
                cpu: "1"
                memory: 1Gi
              requests:
                cpu: "1"
                memory: 1Gi
        volumes:
          - name: worker-bin
            emptyDir: {}
          - name: docker-certs
            emptyDir: {}
          - name: docker-storage
            emptyDir: {}
          # Disk-backed (not tmpfs): repo checkout + gate builds are large.
          # Same absolute path in worker AND dind so `docker run -v`
          # bind-mounts resolve on the daemon's filesystem.
          - name: ci-shared
            emptyDir:
              sizeLimit: 8Gi
  triggers:
    - type: postgresql
      metadata:
        # Resolved by the KEDA operator from ITS namespace, not this one —
        # a bare in-namespace service name never resolves there.
        host: "ploeg-db-rw.default.svc.cluster.local"
        port: "5432"
        userName: "ploeg_scaler"
        dbName: "app"
        sslmode: "require"
        # The one poll-query shape: served index-only by work_items_claimable
        # (backlog #24/#47). Must stay in sync with store.Claim/QueueDepth.
        query: "SELECT COUNT(*) FROM work_items WHERE team = 'zinc' AND state = 'queued' AND (next_eligible_at IS NULL OR next_eligible_at <= now())"
        targetQueryValue: "1"
        activationTargetQueryValue: "0"
      authenticationRef:
        name: ploeg-scaler
---
# Source: ploeg/templates/scaledjob.yaml
# One ScaledJob per (team, Role) — the flagship executor. A team without a
# plan has exactly one Role-less workload, byte-identical to the pre-Shift
# shape; a team with a plan gets one per Role, which is what makes "different
# agents, different harnesses, different images" literally true: pod shape is
# fixed at render time. The shape lives in the shared ploeg.workerPodTemplate
# helper (also used by the CronJob executor): privileged DinD sidecar when the
# harness needs Docker, a shared disk-backed workspace mounted at the SAME
# path in both containers, and zero Kubernetes API authority for the
# LLM-driven process.
apiVersion: keda.sh/v1alpha1
kind: ScaledJob
metadata:
  name: ploeg-worker-lead
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
    # KEDA copies these labels onto every Job it creates, which is the only way
    # the hazard reaches the Job. Kyverno autogens a Job rule for
    # pod-security-baseline-enforce and a Job selector matches the JOB's own
    # labels — stamping this on the pod template alone left the PolicyException
    # unable to admit the Job, and DinD teams were rejected at admission.
    ploeg.webgrip.dev/privileged-dind: "true"
spec:
  pollingInterval: 30
  minReplicaCount: 0
  maxReplicaCount: 1
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  rollout:
    strategy: gradual # spec updates never kill in-flight runs (backlog #50)
  scalingStrategy:
    strategy: accurate # never eager: open over-scaling bug kedacore/keda#6416 (backlog #47)
  jobTargetRef:
    parallelism: 1
    completions: 1
    backoffLimit: 0 # ploeg owns retries; every pod is one auditable run (backlog #51)
    activeDeadlineSeconds: 7200
    template:
      
      
      metadata:
        labels:
          app.kubernetes.io/name: ploeg-worker
          ploeg.webgrip.dev/team: lead
          # Names the HAZARD, not the workload: this pod carries a privileged
          # docker:dind sidecar, and the ploeg-worker PolicyException is keyed on
          # exactly this label. A pod without dind never carries it and is held to
          # the full baseline — so adding a Role costs no security-repo change, and
          # readers are not waived for a privilege they do not take.
          ploeg.webgrip.dev/privileged-dind: "true"
      spec:
        restartPolicy: Never
        # The shutdown budget for ploeg-worker's SIGTERM path: abort the harness,
        # revoke the per-run credential, settle its spend, report the outcome. The
        # 30s default SIGKILLed the pod mid-report, which is the silence that made a
        # killed run indistinguishable from a hung one and cost the Round an attempt.
        terminationGracePeriodSeconds: 90
        # An identity of the workers' own. Naming none left them on `default`, which
        # both trips require-non-default-serviceaccount and makes every workload in
        # the namespace indistinguishable in an audit log. The token stays unmounted
        # either way — a worker needs no Kubernetes API authority at all.
        serviceAccountName: ploeg-worker
        automountServiceAccountToken: false
        nodeSelector:
          node.webgrip.io/pool: worker
        securityContext:
          fsGroup: 1000
        initContainers:
          # Extracts ploeg-worker from the distroless ploegd image via
          # self-copy (no shell in distroless).
          - name: worker-bin
            image: harbor.webgrip.dev/webgrip/ploegd:CHART-VERSION
            command: ["/usr/local/bin/ploeg-worker", "install", "/mnt/bin/ploeg-worker"]
            securityContext:
              runAsNonRoot: true
              runAsUser: 65532
              allowPrivilegeEscalation: false
              capabilities:
                drop: [ALL]
              seccompProfile:
                type: RuntimeDefault
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
            resources:
              limits:
                cpu: 50m
                memory: 64Mi
              requests:
                cpu: 50m
                memory: 64Mi
          # DinD sidecar: the harness sandbox and the repo quality gates need a
          # Docker daemon. Requires the ploeg-worker PolicyException. Teams whose
          # harness needs no Docker set harness.dind: false.
          - name: dind
            image: harbor.webgrip.dev/dockerhub/library/docker:29.6.2-dind
            restartPolicy: Always
            securityContext:
              privileged: true
            env:
              - name: DOCKER_TLS_CERTDIR
                value: /certs
            startupProbe:
              tcpSocket:
                port: 2376
              periodSeconds: 2
              failureThreshold: 60
              timeoutSeconds: 5
            volumeMounts:
              - name: docker-certs
                mountPath: /certs
              - name: docker-storage
                mountPath: /var/lib/docker
              - name: ci-shared
                mountPath: /mnt/ci-shared
            resources:
              limits:
                cpu: "1"
                memory: 1536Mi
              requests:
                cpu: "1"
                memory: 1536Mi
        containers:
          - name: worker
            image: harbor.webgrip.dev/webgrip/opencode-runner:0.1.0
            command: ["/mnt/bin/ploeg-worker"]
            env:
              - name: PLOEG_API_URL
                value: "http://ploeg:8080"
              - name: PLOEG_TEAM
                value: "lead"
              - name: PLOEG_HARNESS
                value: "acp"
              - name: PLOEG_ACP_PROFILE
                value: "custom"
              - name: PLOEG_ACP_ARGV
                value: "[\"opencode\",\"acp\"]"
              - name: PLOEG_ACP_PERMISSION_MODE
                value: "allow_read_only"
              - name: PLOEG_ACP_IDLE_TIMEOUT
                value: "5m"
              - name: LLM_MODEL
                value: "litellm_proxy/claude-sonnet-5"
              - name: LITELLM_KEY_BUDGET
                value: "1"
              - name: LITELLM_KEY_DURATION
                value: "4h"
              - name: LLM_BASE_URL
                value: "http://litellm.ai.svc.cluster.local:4000/v1"
              - name: LITELLM_ADMIN_URL
                value: "http://litellm.ai.svc.cluster.local:4000"
              - name: LITELLM_MASTER_KEY
                valueFrom:
                  secretKeyRef:
                    name: agent-litellm-master
                    key: LITELLM_MASTER_KEY
              - name: PLOEG_TARGET_FORGE
                value: "forgejo"
              - name: FORGE_URL
                value: "http://forgejo-http.forgejo.svc.cluster.local:3000"
              - name: AGENT_BUILDER_TOKEN
                valueFrom:
                  secretKeyRef:
                    name: agent-builder-token
                    key: FORGEJO_TOKEN
              # FALLBACK target only. The repository belongs to the work item,
              # resolved at ingest from its tracker scope and delivered on the claim
              # (R11, ADR-0001); these render only while a team still pins a repo,
              # and a team that declares none renders no repo env at all.
              # Downward API: node+pod identity survives pod/job cleanup for
              # run forensics (VIK-597). Logged at worker start and embedded in
              # every checkpoint.
              - name: NODE_NAME
                valueFrom:
                  fieldRef:
                    fieldPath: spec.nodeName
              - name: POD_UID
                valueFrom:
                  fieldRef:
                    fieldPath: metadata.uid
              - name: WORK_DIR
                value: /mnt/ci-shared
              - name: DOCKER_HOST
                value: tcp://localhost:2376
              - name: DOCKER_CERT_PATH
                value: /certs/client
              - name: DOCKER_TLS_VERIFY
                value: "1"
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
                readOnly: true
              - name: docker-certs
                mountPath: /certs
                readOnly: true
              - name: ci-shared
                mountPath: /mnt/ci-shared
            resources:
              limits:
                cpu: "1"
                memory: 1Gi
              requests:
                cpu: "1"
                memory: 1Gi
        volumes:
          - name: worker-bin
            emptyDir: {}
          - name: docker-certs
            emptyDir: {}
          - name: docker-storage
            emptyDir: {}
          # Disk-backed (not tmpfs): repo checkout + gate builds are large.
          # Same absolute path in worker AND dind so `docker run -v`
          # bind-mounts resolve on the daemon's filesystem.
          - name: ci-shared
            emptyDir:
              sizeLimit: 8Gi
  triggers:
    - type: postgresql
      metadata:
        # Resolved by the KEDA operator from ITS namespace, not this one —
        # a bare in-namespace service name never resolves there.
        host: "ploeg-db-rw.default.svc.cluster.local"
        port: "5432"
        userName: "ploeg_scaler"
        dbName: "app"
        sslmode: "require"
        # The one poll-query shape: served index-only by work_items_claimable
        # (backlog #24/#47). Must stay in sync with store.Claim/QueueDepth.
        query: "SELECT COUNT(*) FROM work_items WHERE team = 'lead' AND state = 'queued' AND (next_eligible_at IS NULL OR next_eligible_at <= now())"
        targetQueryValue: "1"
        activationTargetQueryValue: "0"
      authenticationRef:
        name: ploeg-scaler
---
# Source: ploeg/templates/scaledjob.yaml
# One ScaledJob per (team, Role) — the flagship executor. A team without a
# plan has exactly one Role-less workload, byte-identical to the pre-Shift
# shape; a team with a plan gets one per Role, which is what makes "different
# agents, different harnesses, different images" literally true: pod shape is
# fixed at render time. The shape lives in the shared ploeg.workerPodTemplate
# helper (also used by the CronJob executor): privileged DinD sidecar when the
# harness needs Docker, a shared disk-backed workspace mounted at the SAME
# path in both containers, and zero Kubernetes API authority for the
# LLM-driven process.
apiVersion: keda.sh/v1alpha1
kind: ScaledJob
metadata:
  name: ploeg-worker-gold-analyst
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
spec:
  pollingInterval: 30
  minReplicaCount: 0
  maxReplicaCount: 1
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  rollout:
    strategy: gradual # spec updates never kill in-flight runs (backlog #50)
  scalingStrategy:
    strategy: accurate # never eager: open over-scaling bug kedacore/keda#6416 (backlog #47)
  jobTargetRef:
    parallelism: 1
    completions: 1
    backoffLimit: 0 # ploeg owns retries; every pod is one auditable run (backlog #51)
    activeDeadlineSeconds: 7200
    template:
      
      
      metadata:
        labels:
          app.kubernetes.io/name: ploeg-worker
          ploeg.webgrip.dev/team: gold
          ploeg.webgrip.dev/role: analyst
      spec:
        restartPolicy: Never
        # The shutdown budget for ploeg-worker's SIGTERM path: abort the harness,
        # revoke the per-run credential, settle its spend, report the outcome. The
        # 30s default SIGKILLed the pod mid-report, which is the silence that made a
        # killed run indistinguishable from a hung one and cost the Round an attempt.
        terminationGracePeriodSeconds: 90
        # An identity of the workers' own. Naming none left them on `default`, which
        # both trips require-non-default-serviceaccount and makes every workload in
        # the namespace indistinguishable in an audit log. The token stays unmounted
        # either way — a worker needs no Kubernetes API authority at all.
        serviceAccountName: ploeg-worker
        automountServiceAccountToken: false
        nodeSelector:
          node.webgrip.io/pool: worker
        securityContext:
          fsGroup: 1000
        initContainers:
          # Extracts ploeg-worker from the distroless ploegd image via
          # self-copy (no shell in distroless).
          - name: worker-bin
            image: harbor.webgrip.dev/webgrip/ploegd:CHART-VERSION
            command: ["/usr/local/bin/ploeg-worker", "install", "/mnt/bin/ploeg-worker"]
            securityContext:
              runAsNonRoot: true
              runAsUser: 65532
              allowPrivilegeEscalation: false
              capabilities:
                drop: [ALL]
              seccompProfile:
                type: RuntimeDefault
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
            resources:
              limits:
                cpu: 50m
                memory: 64Mi
              requests:
                cpu: 50m
                memory: 64Mi
        containers:
          - name: worker
            image: harbor.webgrip.dev/webgrip/agent-runner:1.0.2@sha256:086b4bc0d3fda0e3c179f9501bb46a612f5f33e60d96405a5d94471a26279596
            command: ["/mnt/bin/ploeg-worker"]
            env:
              - name: PLOEG_API_URL
                value: "http://ploeg:8080"
              - name: PLOEG_TEAM
                value: "gold"
              - name: PLOEG_ROLE
                value: "analyst"
              - name: PLOEG_HARNESS
                value: "openhands"
              - name: LLM_MODEL
                value: "litellm_proxy/deepseek-chat"
              - name: LITELLM_KEY_BUDGET
                value: "0.50"
              - name: LITELLM_KEY_DURATION
                value: "4h"
              - name: LLM_BASE_URL
                value: "http://litellm.ai.svc.cluster.local:4000/v1"
              - name: LITELLM_ADMIN_URL
                value: "http://litellm.ai.svc.cluster.local:4000"
              - name: LITELLM_MASTER_KEY
                valueFrom:
                  secretKeyRef:
                    name: agent-litellm-master
                    key: LITELLM_MASTER_KEY
              - name: PLOEG_TARGET_FORGE
                value: "forgejo"
              - name: FORGE_URL
                value: "http://forgejo-http.forgejo.svc.cluster.local:3000"
              - name: AGENT_BUILDER_TOKEN
                valueFrom:
                  secretKeyRef:
                    name: agent-reader-token
                    key: FORGEJO_TOKEN
              # FALLBACK target only. The repository belongs to the work item,
              # resolved at ingest from its tracker scope and delivered on the claim
              # (R11, ADR-0001); these render only while a team still pins a repo,
              # and a team that declares none renders no repo env at all.
              # Downward API: node+pod identity survives pod/job cleanup for
              # run forensics (VIK-597). Logged at worker start and embedded in
              # every checkpoint.
              - name: NODE_NAME
                valueFrom:
                  fieldRef:
                    fieldPath: spec.nodeName
              - name: POD_UID
                valueFrom:
                  fieldRef:
                    fieldPath: metadata.uid
              - name: WORK_DIR
                value: /mnt/ci-shared
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
                readOnly: true
              - name: ci-shared
                mountPath: /mnt/ci-shared
            resources:
              limits:
                cpu: "1"
                memory: 1Gi
              requests:
                cpu: "1"
                memory: 1Gi
        volumes:
          - name: worker-bin
            emptyDir: {}
          # Disk-backed (not tmpfs): repo checkout + gate builds are large.
          # Same absolute path in worker AND dind so `docker run -v`
          # bind-mounts resolve on the daemon's filesystem.
          - name: ci-shared
            emptyDir:
              sizeLimit: 8Gi
  triggers:
    - type: postgresql
      metadata:
        # Resolved by the KEDA operator from ITS namespace, not this one —
        # a bare in-namespace service name never resolves there.
        host: "ploeg-db-rw.default.svc.cluster.local"
        port: "5432"
        userName: "ploeg_scaler"
        dbName: "app"
        sslmode: "require"
        # THE claim predicate for a Role, and it must stay byte-identical to
        # store.ClaimRole and store.PendingRuns: pending runs for this team and
        # role. Served index-only by agent_runs_claimable, which migration 0008
        # created verbatim for this query:
        #   CREATE INDEX agent_runs_claimable
        #       ON agent_runs (team, role, id) WHERE state = 'pending';
        # Overshoot merely wastes a pod that exits 0 empty-handed; undershoot
        # stalls Work Items with no error anywhere, which is the worst failure
        # this system can have. TestClaimRoleAgreesWithPendingRuns keeps the
        # two Go callers honest; this comment is the third copy's leash.
        query: "SELECT COUNT(*) FROM agent_runs WHERE team = 'gold' AND role = 'analyst' AND state = 'pending'"
        targetQueryValue: "1"
        activationTargetQueryValue: "0"
      authenticationRef:
        name: ploeg-scaler
---
# Source: ploeg/templates/scaledjob.yaml
# One ScaledJob per (team, Role) — the flagship executor. A team without a
# plan has exactly one Role-less workload, byte-identical to the pre-Shift
# shape; a team with a plan gets one per Role, which is what makes "different
# agents, different harnesses, different images" literally true: pod shape is
# fixed at render time. The shape lives in the shared ploeg.workerPodTemplate
# helper (also used by the CronJob executor): privileged DinD sidecar when the
# harness needs Docker, a shared disk-backed workspace mounted at the SAME
# path in both containers, and zero Kubernetes API authority for the
# LLM-driven process.
apiVersion: keda.sh/v1alpha1
kind: ScaledJob
metadata:
  name: ploeg-worker-gold-tests
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
spec:
  pollingInterval: 30
  minReplicaCount: 0
  maxReplicaCount: 1
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  rollout:
    strategy: gradual # spec updates never kill in-flight runs (backlog #50)
  scalingStrategy:
    strategy: accurate # never eager: open over-scaling bug kedacore/keda#6416 (backlog #47)
  jobTargetRef:
    parallelism: 1
    completions: 1
    backoffLimit: 0 # ploeg owns retries; every pod is one auditable run (backlog #51)
    activeDeadlineSeconds: 7200
    template:
      
      
      metadata:
        labels:
          app.kubernetes.io/name: ploeg-worker
          ploeg.webgrip.dev/team: gold
          ploeg.webgrip.dev/role: tests
      spec:
        restartPolicy: Never
        # The shutdown budget for ploeg-worker's SIGTERM path: abort the harness,
        # revoke the per-run credential, settle its spend, report the outcome. The
        # 30s default SIGKILLed the pod mid-report, which is the silence that made a
        # killed run indistinguishable from a hung one and cost the Round an attempt.
        terminationGracePeriodSeconds: 90
        # An identity of the workers' own. Naming none left them on `default`, which
        # both trips require-non-default-serviceaccount and makes every workload in
        # the namespace indistinguishable in an audit log. The token stays unmounted
        # either way — a worker needs no Kubernetes API authority at all.
        serviceAccountName: ploeg-worker
        automountServiceAccountToken: false
        nodeSelector:
          node.webgrip.io/pool: worker
        securityContext:
          fsGroup: 1000
        initContainers:
          # Extracts ploeg-worker from the distroless ploegd image via
          # self-copy (no shell in distroless).
          - name: worker-bin
            image: harbor.webgrip.dev/webgrip/ploegd:CHART-VERSION
            command: ["/usr/local/bin/ploeg-worker", "install", "/mnt/bin/ploeg-worker"]
            securityContext:
              runAsNonRoot: true
              runAsUser: 65532
              allowPrivilegeEscalation: false
              capabilities:
                drop: [ALL]
              seccompProfile:
                type: RuntimeDefault
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
            resources:
              limits:
                cpu: 50m
                memory: 64Mi
              requests:
                cpu: 50m
                memory: 64Mi
        containers:
          - name: worker
            image: harbor.webgrip.dev/webgrip/agent-runner:1.0.2@sha256:086b4bc0d3fda0e3c179f9501bb46a612f5f33e60d96405a5d94471a26279596
            command: ["/mnt/bin/ploeg-worker"]
            env:
              - name: PLOEG_API_URL
                value: "http://ploeg:8080"
              - name: PLOEG_TEAM
                value: "gold"
              - name: PLOEG_ROLE
                value: "tests"
              - name: PLOEG_HARNESS
                value: "openhands"
              - name: LLM_MODEL
                value: "litellm_proxy/deepseek-chat"
              - name: LITELLM_KEY_BUDGET
                value: "0.50"
              - name: LITELLM_KEY_DURATION
                value: "4h"
              - name: LLM_BASE_URL
                value: "http://litellm.ai.svc.cluster.local:4000/v1"
              - name: LITELLM_ADMIN_URL
                value: "http://litellm.ai.svc.cluster.local:4000"
              - name: LITELLM_MASTER_KEY
                valueFrom:
                  secretKeyRef:
                    name: agent-litellm-master
                    key: LITELLM_MASTER_KEY
              - name: PLOEG_TARGET_FORGE
                value: "forgejo"
              - name: FORGE_URL
                value: "http://forgejo-http.forgejo.svc.cluster.local:3000"
              - name: AGENT_BUILDER_TOKEN
                valueFrom:
                  secretKeyRef:
                    name: agent-reader-token
                    key: FORGEJO_TOKEN
              # FALLBACK target only. The repository belongs to the work item,
              # resolved at ingest from its tracker scope and delivered on the claim
              # (R11, ADR-0001); these render only while a team still pins a repo,
              # and a team that declares none renders no repo env at all.
              # Downward API: node+pod identity survives pod/job cleanup for
              # run forensics (VIK-597). Logged at worker start and embedded in
              # every checkpoint.
              - name: NODE_NAME
                valueFrom:
                  fieldRef:
                    fieldPath: spec.nodeName
              - name: POD_UID
                valueFrom:
                  fieldRef:
                    fieldPath: metadata.uid
              - name: WORK_DIR
                value: /mnt/ci-shared
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
                readOnly: true
              - name: ci-shared
                mountPath: /mnt/ci-shared
            resources:
              limits:
                cpu: "1"
                memory: 1Gi
              requests:
                cpu: "1"
                memory: 1Gi
        volumes:
          - name: worker-bin
            emptyDir: {}
          # Disk-backed (not tmpfs): repo checkout + gate builds are large.
          # Same absolute path in worker AND dind so `docker run -v`
          # bind-mounts resolve on the daemon's filesystem.
          - name: ci-shared
            emptyDir:
              sizeLimit: 8Gi
  triggers:
    - type: postgresql
      metadata:
        # Resolved by the KEDA operator from ITS namespace, not this one —
        # a bare in-namespace service name never resolves there.
        host: "ploeg-db-rw.default.svc.cluster.local"
        port: "5432"
        userName: "ploeg_scaler"
        dbName: "app"
        sslmode: "require"
        # THE claim predicate for a Role, and it must stay byte-identical to
        # store.ClaimRole and store.PendingRuns: pending runs for this team and
        # role. Served index-only by agent_runs_claimable, which migration 0008
        # created verbatim for this query:
        #   CREATE INDEX agent_runs_claimable
        #       ON agent_runs (team, role, id) WHERE state = 'pending';
        # Overshoot merely wastes a pod that exits 0 empty-handed; undershoot
        # stalls Work Items with no error anywhere, which is the worst failure
        # this system can have. TestClaimRoleAgreesWithPendingRuns keeps the
        # two Go callers honest; this comment is the third copy's leash.
        query: "SELECT COUNT(*) FROM agent_runs WHERE team = 'gold' AND role = 'tests' AND state = 'pending'"
        targetQueryValue: "1"
        activationTargetQueryValue: "0"
      authenticationRef:
        name: ploeg-scaler
---
# Source: ploeg/templates/scaledjob.yaml
# One ScaledJob per (team, Role) — the flagship executor. A team without a
# plan has exactly one Role-less workload, byte-identical to the pre-Shift
# shape; a team with a plan gets one per Role, which is what makes "different
# agents, different harnesses, different images" literally true: pod shape is
# fixed at render time. The shape lives in the shared ploeg.workerPodTemplate
# helper (also used by the CronJob executor): privileged DinD sidecar when the
# harness needs Docker, a shared disk-backed workspace mounted at the SAME
# path in both containers, and zero Kubernetes API authority for the
# LLM-driven process.
apiVersion: keda.sh/v1alpha1
kind: ScaledJob
metadata:
  name: ploeg-worker-gold-builder
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
    # KEDA copies these labels onto every Job it creates, which is the only way
    # the hazard reaches the Job. Kyverno autogens a Job rule for
    # pod-security-baseline-enforce and a Job selector matches the JOB's own
    # labels — stamping this on the pod template alone left the PolicyException
    # unable to admit the Job, and DinD teams were rejected at admission.
    ploeg.webgrip.dev/privileged-dind: "true"
spec:
  pollingInterval: 30
  minReplicaCount: 0
  maxReplicaCount: 1
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  rollout:
    strategy: gradual # spec updates never kill in-flight runs (backlog #50)
  scalingStrategy:
    strategy: accurate # never eager: open over-scaling bug kedacore/keda#6416 (backlog #47)
  jobTargetRef:
    parallelism: 1
    completions: 1
    backoffLimit: 0 # ploeg owns retries; every pod is one auditable run (backlog #51)
    activeDeadlineSeconds: 7200
    template:
      
      
      metadata:
        labels:
          app.kubernetes.io/name: ploeg-worker
          ploeg.webgrip.dev/team: gold
          ploeg.webgrip.dev/role: builder
          # Names the HAZARD, not the workload: this pod carries a privileged
          # docker:dind sidecar, and the ploeg-worker PolicyException is keyed on
          # exactly this label. A pod without dind never carries it and is held to
          # the full baseline — so adding a Role costs no security-repo change, and
          # readers are not waived for a privilege they do not take.
          ploeg.webgrip.dev/privileged-dind: "true"
      spec:
        restartPolicy: Never
        # The shutdown budget for ploeg-worker's SIGTERM path: abort the harness,
        # revoke the per-run credential, settle its spend, report the outcome. The
        # 30s default SIGKILLed the pod mid-report, which is the silence that made a
        # killed run indistinguishable from a hung one and cost the Round an attempt.
        terminationGracePeriodSeconds: 90
        # An identity of the workers' own. Naming none left them on `default`, which
        # both trips require-non-default-serviceaccount and makes every workload in
        # the namespace indistinguishable in an audit log. The token stays unmounted
        # either way — a worker needs no Kubernetes API authority at all.
        serviceAccountName: ploeg-worker
        automountServiceAccountToken: false
        nodeSelector:
          node.webgrip.io/pool: worker
        securityContext:
          fsGroup: 1000
        initContainers:
          # Extracts ploeg-worker from the distroless ploegd image via
          # self-copy (no shell in distroless).
          - name: worker-bin
            image: harbor.webgrip.dev/webgrip/ploegd:CHART-VERSION
            command: ["/usr/local/bin/ploeg-worker", "install", "/mnt/bin/ploeg-worker"]
            securityContext:
              runAsNonRoot: true
              runAsUser: 65532
              allowPrivilegeEscalation: false
              capabilities:
                drop: [ALL]
              seccompProfile:
                type: RuntimeDefault
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
            resources:
              limits:
                cpu: 50m
                memory: 64Mi
              requests:
                cpu: 50m
                memory: 64Mi
          # DinD sidecar: the harness sandbox and the repo quality gates need a
          # Docker daemon. Requires the ploeg-worker PolicyException. Teams whose
          # harness needs no Docker set harness.dind: false.
          - name: dind
            image: harbor.webgrip.dev/dockerhub/library/docker:29.6.2-dind
            restartPolicy: Always
            securityContext:
              privileged: true
            env:
              - name: DOCKER_TLS_CERTDIR
                value: /certs
            startupProbe:
              tcpSocket:
                port: 2376
              periodSeconds: 2
              failureThreshold: 60
              timeoutSeconds: 5
            volumeMounts:
              - name: docker-certs
                mountPath: /certs
              - name: docker-storage
                mountPath: /var/lib/docker
              - name: ci-shared
                mountPath: /mnt/ci-shared
            resources:
              limits:
                cpu: "1"
                memory: 1536Mi
              requests:
                cpu: "1"
                memory: 1536Mi
        containers:
          - name: worker
            image: harbor.webgrip.dev/webgrip/agent-runner:1.0.2@sha256:086b4bc0d3fda0e3c179f9501bb46a612f5f33e60d96405a5d94471a26279596
            command: ["/mnt/bin/ploeg-worker"]
            env:
              - name: PLOEG_API_URL
                value: "http://ploeg:8080"
              - name: PLOEG_TEAM
                value: "gold"
              - name: PLOEG_ROLE
                value: "builder"
              - name: PLOEG_HARNESS
                value: "openhands"
              - name: LLM_MODEL
                value: "litellm_proxy/claude-sonnet-5"
              - name: LITELLM_KEY_BUDGET
                value: "3.00"
              - name: LITELLM_KEY_DURATION
                value: "4h"
              - name: LLM_BASE_URL
                value: "http://litellm.ai.svc.cluster.local:4000/v1"
              - name: LITELLM_ADMIN_URL
                value: "http://litellm.ai.svc.cluster.local:4000"
              - name: LITELLM_MASTER_KEY
                valueFrom:
                  secretKeyRef:
                    name: agent-litellm-master
                    key: LITELLM_MASTER_KEY
              - name: PLOEG_TARGET_FORGE
                value: "forgejo"
              - name: FORGE_URL
                value: "http://forgejo-http.forgejo.svc.cluster.local:3000"
              - name: AGENT_BUILDER_TOKEN
                valueFrom:
                  secretKeyRef:
                    name: agent-builder-token
                    key: FORGEJO_TOKEN
              # FALLBACK target only. The repository belongs to the work item,
              # resolved at ingest from its tracker scope and delivered on the claim
              # (R11, ADR-0001); these render only while a team still pins a repo,
              # and a team that declares none renders no repo env at all.
              # Downward API: node+pod identity survives pod/job cleanup for
              # run forensics (VIK-597). Logged at worker start and embedded in
              # every checkpoint.
              - name: NODE_NAME
                valueFrom:
                  fieldRef:
                    fieldPath: spec.nodeName
              - name: POD_UID
                valueFrom:
                  fieldRef:
                    fieldPath: metadata.uid
              - name: WORK_DIR
                value: /mnt/ci-shared
              - name: DOCKER_HOST
                value: tcp://localhost:2376
              - name: DOCKER_CERT_PATH
                value: /certs/client
              - name: DOCKER_TLS_VERIFY
                value: "1"
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
                readOnly: true
              - name: docker-certs
                mountPath: /certs
                readOnly: true
              - name: ci-shared
                mountPath: /mnt/ci-shared
            resources:
              limits:
                cpu: "1"
                memory: 1Gi
              requests:
                cpu: "1"
                memory: 1Gi
        volumes:
          - name: worker-bin
            emptyDir: {}
          - name: docker-certs
            emptyDir: {}
          - name: docker-storage
            emptyDir: {}
          # Disk-backed (not tmpfs): repo checkout + gate builds are large.
          # Same absolute path in worker AND dind so `docker run -v`
          # bind-mounts resolve on the daemon's filesystem.
          - name: ci-shared
            emptyDir:
              sizeLimit: 8Gi
  triggers:
    - type: postgresql
      metadata:
        # Resolved by the KEDA operator from ITS namespace, not this one —
        # a bare in-namespace service name never resolves there.
        host: "ploeg-db-rw.default.svc.cluster.local"
        port: "5432"
        userName: "ploeg_scaler"
        dbName: "app"
        sslmode: "require"
        # THE claim predicate for a Role, and it must stay byte-identical to
        # store.ClaimRole and store.PendingRuns: pending runs for this team and
        # role. Served index-only by agent_runs_claimable, which migration 0008
        # created verbatim for this query:
        #   CREATE INDEX agent_runs_claimable
        #       ON agent_runs (team, role, id) WHERE state = 'pending';
        # Overshoot merely wastes a pod that exits 0 empty-handed; undershoot
        # stalls Work Items with no error anywhere, which is the worst failure
        # this system can have. TestClaimRoleAgreesWithPendingRuns keeps the
        # two Go callers honest; this comment is the third copy's leash.
        query: "SELECT COUNT(*) FROM agent_runs WHERE team = 'gold' AND role = 'builder' AND state = 'pending'"
        targetQueryValue: "1"
        activationTargetQueryValue: "0"
      authenticationRef:
        name: ploeg-scaler
---
# Source: ploeg/templates/scaledjob.yaml
# One ScaledJob per (team, Role) — the flagship executor. A team without a
# plan has exactly one Role-less workload, byte-identical to the pre-Shift
# shape; a team with a plan gets one per Role, which is what makes "different
# agents, different harnesses, different images" literally true: pod shape is
# fixed at render time. The shape lives in the shared ploeg.workerPodTemplate
# helper (also used by the CronJob executor): privileged DinD sidecar when the
# harness needs Docker, a shared disk-backed workspace mounted at the SAME
# path in both containers, and zero Kubernetes API authority for the
# LLM-driven process.
apiVersion: keda.sh/v1alpha1
kind: ScaledJob
metadata:
  name: ploeg-worker-gold-reviewer
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
spec:
  pollingInterval: 30
  minReplicaCount: 0
  maxReplicaCount: 1
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  rollout:
    strategy: gradual # spec updates never kill in-flight runs (backlog #50)
  scalingStrategy:
    strategy: accurate # never eager: open over-scaling bug kedacore/keda#6416 (backlog #47)
  jobTargetRef:
    parallelism: 1
    completions: 1
    backoffLimit: 0 # ploeg owns retries; every pod is one auditable run (backlog #51)
    activeDeadlineSeconds: 7200
    template:
      
      
      metadata:
        labels:
          app.kubernetes.io/name: ploeg-worker
          ploeg.webgrip.dev/team: gold
          ploeg.webgrip.dev/role: reviewer
      spec:
        restartPolicy: Never
        # The shutdown budget for ploeg-worker's SIGTERM path: abort the harness,
        # revoke the per-run credential, settle its spend, report the outcome. The
        # 30s default SIGKILLed the pod mid-report, which is the silence that made a
        # killed run indistinguishable from a hung one and cost the Round an attempt.
        terminationGracePeriodSeconds: 90
        # An identity of the workers' own. Naming none left them on `default`, which
        # both trips require-non-default-serviceaccount and makes every workload in
        # the namespace indistinguishable in an audit log. The token stays unmounted
        # either way — a worker needs no Kubernetes API authority at all.
        serviceAccountName: ploeg-worker
        automountServiceAccountToken: false
        nodeSelector:
          node.webgrip.io/pool: worker
        securityContext:
          fsGroup: 1000
        initContainers:
          # Extracts ploeg-worker from the distroless ploegd image via
          # self-copy (no shell in distroless).
          - name: worker-bin
            image: harbor.webgrip.dev/webgrip/ploegd:CHART-VERSION
            command: ["/usr/local/bin/ploeg-worker", "install", "/mnt/bin/ploeg-worker"]
            securityContext:
              runAsNonRoot: true
              runAsUser: 65532
              allowPrivilegeEscalation: false
              capabilities:
                drop: [ALL]
              seccompProfile:
                type: RuntimeDefault
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
            resources:
              limits:
                cpu: 50m
                memory: 64Mi
              requests:
                cpu: 50m
                memory: 64Mi
        containers:
          - name: worker
            image: harbor.webgrip.dev/webgrip/agent-runner:1.0.2@sha256:086b4bc0d3fda0e3c179f9501bb46a612f5f33e60d96405a5d94471a26279596
            command: ["/mnt/bin/ploeg-worker"]
            env:
              - name: PLOEG_API_URL
                value: "http://ploeg:8080"
              - name: PLOEG_TEAM
                value: "gold"
              - name: PLOEG_ROLE
                value: "reviewer"
              - name: PLOEG_HARNESS
                value: "openhands"
              - name: LLM_MODEL
                value: "litellm_proxy/claude-sonnet-5"
              - name: LITELLM_KEY_BUDGET
                value: "0.75"
              - name: LITELLM_KEY_DURATION
                value: "4h"
              - name: LLM_BASE_URL
                value: "http://litellm.ai.svc.cluster.local:4000/v1"
              - name: LITELLM_ADMIN_URL
                value: "http://litellm.ai.svc.cluster.local:4000"
              - name: LITELLM_MASTER_KEY
                valueFrom:
                  secretKeyRef:
                    name: agent-litellm-master
                    key: LITELLM_MASTER_KEY
              - name: PLOEG_TARGET_FORGE
                value: "forgejo"
              - name: FORGE_URL
                value: "http://forgejo-http.forgejo.svc.cluster.local:3000"
              - name: AGENT_BUILDER_TOKEN
                valueFrom:
                  secretKeyRef:
                    name: agent-reader-token
                    key: FORGEJO_TOKEN
              # FALLBACK target only. The repository belongs to the work item,
              # resolved at ingest from its tracker scope and delivered on the claim
              # (R11, ADR-0001); these render only while a team still pins a repo,
              # and a team that declares none renders no repo env at all.
              # Downward API: node+pod identity survives pod/job cleanup for
              # run forensics (VIK-597). Logged at worker start and embedded in
              # every checkpoint.
              - name: NODE_NAME
                valueFrom:
                  fieldRef:
                    fieldPath: spec.nodeName
              - name: POD_UID
                valueFrom:
                  fieldRef:
                    fieldPath: metadata.uid
              - name: WORK_DIR
                value: /mnt/ci-shared
            volumeMounts:
              - name: worker-bin
                mountPath: /mnt/bin
                readOnly: true
              - name: ci-shared
                mountPath: /mnt/ci-shared
            resources:
              limits:
                cpu: "1"
                memory: 1Gi
              requests:
                cpu: "1"
                memory: 1Gi
        volumes:
          - name: worker-bin
            emptyDir: {}
          # Disk-backed (not tmpfs): repo checkout + gate builds are large.
          # Same absolute path in worker AND dind so `docker run -v`
          # bind-mounts resolve on the daemon's filesystem.
          - name: ci-shared
            emptyDir:
              sizeLimit: 8Gi
  triggers:
    - type: postgresql
      metadata:
        # Resolved by the KEDA operator from ITS namespace, not this one —
        # a bare in-namespace service name never resolves there.
        host: "ploeg-db-rw.default.svc.cluster.local"
        port: "5432"
        userName: "ploeg_scaler"
        dbName: "app"
        sslmode: "require"
        # THE claim predicate for a Role, and it must stay byte-identical to
        # store.ClaimRole and store.PendingRuns: pending runs for this team and
        # role. Served index-only by agent_runs_claimable, which migration 0008
        # created verbatim for this query:
        #   CREATE INDEX agent_runs_claimable
        #       ON agent_runs (team, role, id) WHERE state = 'pending';
        # Overshoot merely wastes a pod that exits 0 empty-handed; undershoot
        # stalls Work Items with no error anywhere, which is the worst failure
        # this system can have. TestClaimRoleAgreesWithPendingRuns keeps the
        # two Go callers honest; this comment is the third copy's leash.
        query: "SELECT COUNT(*) FROM agent_runs WHERE team = 'gold' AND role = 'reviewer' AND state = 'pending'"
        targetQueryValue: "1"
        activationTargetQueryValue: "0"
      authenticationRef:
        name: ploeg-scaler

---
# Source: ploeg/templates/triggerauthentication.yaml
apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: ploeg-scaler
  labels:
    app.kubernetes.io/name: ploeg
    app.kubernetes.io/instance: ploeg
    app.kubernetes.io/version: "CHART-VERSION"
    helm.sh/chart: ploeg-CHART-VERSION
    app.kubernetes.io/managed-by: Helm
spec:
  secretTargetRef:
    - parameter: password
      name: ploeg-scaler
      key: password
