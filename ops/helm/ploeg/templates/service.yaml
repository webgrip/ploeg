apiVersion: v1
kind: Service
metadata:
  name: {{ include "ploeg.fullname" . }}
  labels: {{- include "ploeg.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector: {{- include "ploeg.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
