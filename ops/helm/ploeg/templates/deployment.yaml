apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ploeg.fullname" . }}
  labels: {{- include "ploeg.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels: {{- include "ploeg.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels: {{- include "ploeg.selectorLabels" . | nindent 8 }}
      {{- if .Values.config }}
      annotations:
        # Config is read once at boot, so a change has to restart the pod.
        ploeg.webgrip.dev/config-checksum: {{ toYaml .Values.config | sha256sum | trunc 16 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "ploeg.fullname" . }}
      automountServiceAccountToken: false
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: ploegd
          image: {{ include "ploeg.image" . }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          ports:
            - name: http
              containerPort: 8080
          {{- if .Values.config }}
          volumeMounts:
            - name: config
              mountPath: /etc/ploeg
              readOnly: true
          {{- end }}
          env:
            {{- if .Values.config }}
            - name: PLOEG_CONFIG
              value: /etc/ploeg/ploeg.yaml
            {{- end }}
            - name: PLOEG_LISTEN
              value: ":8080"
            - name: PLOEG_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.database.existingSecret.name }}
                  key: {{ .Values.database.existingSecret.key }}
            - name: PLOEG_VIKUNJA_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.webhook.existingSecret.name }}
                  key: {{ .Values.webhook.existingSecret.key }}
            {{- /* Team plans (run-multi-agent-shifts): ploegd's shift engine
                 reads the same plan the workload templates partition on, so
                 there is exactly one source of truth — executor.teams[].plan.
                 The pool is the team's budget value. Only planned teams are
                 serialised; no plans, no env, and dispatch is unchanged. */}}
            {{- $plans := dict }}
            {{- range .Values.executor.teams }}
            {{- if .plan }}
            {{- $_ := set $plans .name (dict "pool" (.budget | default "0") "rounds" .plan "maxFixRounds" (.maxFixRounds | default 0)) }}
            {{- end }}
            {{- end }}
            {{- if $plans }}
            - name: PLOEG_TEAM_PLANS
              value: {{ toJson $plans | quote }}
            {{- end }}
            {{- /* The forge seam: ploegd publishes findings to the pull
                 request as the same bot that opens them (ADR-0011). Commenting
                 is not pushing, so no second credential — but without a URL
                 there is no ForgeProvider and findings stay in the database. */}}
            {{- with .Values.executor.forgejo }}
            - name: PLOEG_FORGEJO_URL
              value: {{ .url | quote }}
            - name: PLOEG_FORGEJO_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .tokenSecret.name }}
                  key: {{ .tokenSecret.key }}
            {{- /* ADR-0013 tier 2: the credential that MINTS per-run push
                 tokens. Held only by ploegd, never by a worker (R6) — the
                 same escalation ADR-0008 accepts for the LiteLLM master key.
                 Unset = nothing is minted and workers keep the shared token. */}}
            {{- with .adminTokenSecret }}
            - name: PLOEG_FORGEJO_ADMIN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .name }}
                  key: {{ .key }}
            {{- end }}
            {{- with .botUser }}
            - name: PLOEG_FORGEJO_BOT
              value: {{ . | quote }}
            {{- end }}
            {{- end }}
            {{- /* The same seam for GitLab. ploegd's GitLab provider comments
                 on a merge request and verifies inbound webhooks; it does not
                 open one, which is the worker's job. Rendered only when a url
                 is set, so the default (empty) block adds nothing — the same
                 "no URL, no ForgeProvider" rule as forgejo above.

                 PLOEG_TARGET_FORGE names the forge a work item gets when its
                 own target names none, and must agree with executor.forge:
                 disagreeing means ploegd publishes findings to one forge while
                 the worker opened the change request on the other. */}}
            {{- with .Values.executor.gitlab }}
            {{- if .url }}
            - name: PLOEG_GITLAB_URL
              value: {{ .url | quote }}
            - name: PLOEG_GITLAB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .tokenSecret.name }}
                  key: {{ .tokenSecret.key }}
            {{- with .webhookSecret }}
            - name: PLOEG_GITLAB_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .name }}
                  key: {{ .key }}
            {{- end }}
            {{- end }}
            {{- end }}
            {{- if eq (.Values.executor.forge | default "forgejo") "gitlab" }}
            - name: PLOEG_TARGET_FORGE
              value: "gitlab"
            {{- end }}
            {{- /* Tracker write-backs (backlog #31): how a closed Shift asks a
                 person to merge. Unset = the provider keeps its logging no-op
                 and runs still finish; the board just is not updated. */}}
            {{- with .Values.tracker }}
            {{- if .url }}
            - name: PLOEG_VIKUNJA_URL
              value: {{ .url | quote }}
            {{- end }}
            {{- if .tokenSecret }}
            - name: PLOEG_VIKUNJA_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .tokenSecret.name }}
                  key: {{ .tokenSecret.key }}
            {{- end }}
            {{- end }}
            {{- range $k, $v := .Values.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
            {{- with .Values.executor.litellm }}
            - name: LITELLM_ADMIN_URL
              value: {{ .adminUrl | quote }}
            - name: LITELLM_MASTER_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .masterKeySecret.name }}
                  key: {{ .masterKeySecret.key }}
            {{- end }}
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 20
          resources: {{- toYaml .Values.resources | nindent 12 }}
      {{- if .Values.config }}
      volumes:
        - name: config
          configMap:
            name: {{ include "ploeg.fullname" . }}-config
      {{- end }}
